[package]
name = "fmod-sys"
version = "2.2.20"
edition = "2021"

build = "build.rs"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
num_enum = "0.7.2"

[build-dependencies]
bindgen = "0.69.2"
cc = "1.0"

[features]
force-debug = []
